{
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api/app"
        }
    ]
    "functions": {
        "api/app.py": {
            "maxDuration": 30 // This function can run for a maximum of 30 seconds
        }
    }
}
